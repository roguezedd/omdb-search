<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
    each file in assets.length ? assets : []
        if /\.css$/.test(file.name)
            <link rel="stylesheet" href='assets/#{file.name}'>

    if styleTags
        | !{styleTags}
</head>
<body>
    <input id="hiddenState" type="hidden" value="#{state}" />
    <div class="main-container">!{html}</div>
    each file in assets.length ? assets : []
        if /\.js$/.test(file.name)
            <script defer src='assets/#{file.name}'></script>
</body>
</html>
