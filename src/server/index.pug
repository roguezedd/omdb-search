<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
    each file in assets.length ? assets : []
        if /\.css$/.test(file.name)
            <link rel="stylesheet" href='assets/#{file.name}'>

    if styleTags
        | !{styleTags}
</head>
<body>
    <div style="display:none" class="state">#{state}</div>
    <div class="main-container">!{html}</div>
    <div style="position:fixed; bottom:0; width: 100%; background:#CCCCCC">renderToString: #{rtsPerf}ms</div>
    each file in assets.length ? assets : []
        if /\.js$/.test(file.name)
            <script defer src='assets/#{file.name}'></script>
</body>
</html>
